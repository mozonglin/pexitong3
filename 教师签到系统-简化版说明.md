# 教师签到系统 - 管理员查看功能（简化版）

## 概述

本系统实现了教师上课签到的管理员查看功能，**仅限单一学院使用**，不包含任何院系相关功能。系统严格按照权限控制，只有校级管理员和超级管理员才能查看教师签到信息。

## 重要说明

1. **不修改现有课程表结构** - 使用您现有的 `courses` 表
2. **无院系功能** - 系统设计为单一学院使用，不包含任何院系字段
3. **只实现查看功能** - 教师签到功能将在其他后端实现

## 数据库设计

### 新增表结构

#### 1. teacher_attendance_records (教师签到记录表)
```sql
CREATE TABLE teacher_attendance_records (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    course_id BIGINT NOT NULL COMMENT '课程ID',
    teacher_id VARCHAR(50) NOT NULL COMMENT '教师ID',
    teacher_name VARCHAR(50) NOT NULL COMMENT '教师姓名',
    
    -- 签到信息
    attendance_status ENUM('completed', 'pending', 'late', 'missed') NOT NULL DEFAULT 'pending',
    attendance_time TIMESTAMP NULL,
    attendance_location VARCHAR(200),
    attendance_note TEXT,
    
    -- 课程信息（冗余存储，提高查询效率）
    course_name VARCHAR(100) NOT NULL,
    class_name VARCHAR(50) NOT NULL,
    classroom VARCHAR(100),
    class_date DATE NOT NULL,
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE,
    FOREIGN KEY (teacher_id) REFERENCES users(id) ON DELETE CASCADE
);
```

#### 2. teacher_attendance_photos (签到照片表)
```sql
CREATE TABLE teacher_attendance_photos (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    attendance_record_id BIGINT NOT NULL,
    course_id BIGINT NOT NULL,
    teacher_id VARCHAR(50) NOT NULL,
    
    -- 照片信息
    original_name VARCHAR(255) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_path VARCHAR(500) NOT NULL,
    file_size BIGINT NOT NULL,
    file_type VARCHAR(100) NOT NULL,
    
    upload_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (attendance_record_id) REFERENCES teacher_attendance_records(id) ON DELETE CASCADE
);
```

#### 3. teacher_attendance_statistics (签到统计表)
```sql
CREATE TABLE teacher_attendance_statistics (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    stat_date DATE NOT NULL,
    stat_type ENUM('day', 'week', 'month') NOT NULL,
    school VARCHAR(100) NOT NULL,
    
    -- 统计数据
    total_courses INT DEFAULT 0,
    completed_count INT DEFAULT 0,
    pending_count INT DEFAULT 0,
    late_count INT DEFAULT 0,
    missed_count INT DEFAULT 0,
    attendance_rate DECIMAL(5,2) DEFAULT 0.00,
    on_time_rate DECIMAL(5,2) DEFAULT 0.00,
    total_teachers INT DEFAULT 0,
    active_teachers INT DEFAULT 0
);
```

## API接口

### 1. 获取课程列表
```
GET /teaching/courses?page=1&pageSize=12&date=2024-12-19&status=completed&search=数学
```

### 2. 获取课程详情
```
GET /teaching/courses/{courseId}
```

### 3. 获取统计数据
```
GET /teaching/statistics?date=2024-12-19&period=day
```

### 4. 获取签到照片
```
GET /teaching/courses/{courseId}/attendance/photo
GET /teaching/courses/{courseId}/attendance/photos
```

### 5. 获取签到历史
```
GET /teaching/attendance/history?period=month&page=1&pageSize=20
```

## 权限控制

- **超级管理员 (super_admin)**: 可以查看所有学校的教师签到数据
- **校级管理员 (school_admin)**: 只能查看本校的教师签到数据
- **其他用户**: 无权访问

## 安装步骤

### 1. 运行数据库初始化
```bash
mysql -u root -p pexitong2 < teaching_system_init.sql
```

### 2. 配置文件
在 `application.properties` 中已添加配置：
```properties
# 教学系统文件存储配置
teaching.file.base-path=F:/teaching_files
teaching.file.url-prefix=/api/teaching/files
```

### 3. 创建文件存储目录
```
F:/teaching_files/
├── attendance_photos/     # 签到照片
├── attendance_reports/    # 签到报告
└── downloads/            # 下载文件临时目录
```

## 技术特点

1. **简化设计** - 无院系概念，适合单一学院使用
2. **不修改现有表** - 兼容您现有的课程表结构
3. **权限隔离** - 严格的管理员权限控制
4. **性能优化** - 分页查询、索引优化、统计缓存
5. **扩展性好** - 模块化设计，易于扩展

## 注意事项

1. **课程表兼容** - 系统会自动从您现有的 `courses` 表创建签到记录
2. **教师签到功能** - 本系统只实现查看功能，签到功能需要在其他后端实现
3. **单一学院** - 系统设计为单一学院使用，如需多院系支持需要重新设计
4. **文件下载** - 照片压缩包和报告下载功能预留了接口，可根据需求完善

## 错误处理

系统提供统一的错误响应格式：
```json
{
  "code": 403,
  "message": "权限不足，只有校级管理员和超级管理员才能查看教师签到信息",
  "data": null,
  "timestamp": "2024-12-19T10:00:00Z"
}
```

## 联系支持

如有问题，请检查：
1. 数据库连接是否正常
2. 用户权限是否正确设置
3. 配置文件路径是否正确
4. 文件存储目录是否已创建




