# 教师签到系统 - 管理员查看功能

## 概述

本系统实现了教师上课签到的管理员查看功能，符合API文档中的所有要求。系统严格按照权限控制，只有校级管理员和超级管理员才能查看教师签到信息，且校级管理员只能查看本校的数据。

## 功能特性

### 1. 权限控制
- **超级管理员 (super_admin)**: 可以查看所有学校的教师签到数据
- **校级管理员 (school_admin)**: 只能查看本校的教师签到数据
- **其他用户**: 无权访问教师签到系统

### 2. 核心功能
1. **课程列表查看** - 支持分页、筛选、搜索
2. **课程详情查看** - 查看具体课程的签到详情
3. **统计数据** - 查看签到率、迟到率等统计信息
4. **签到照片管理** - 查看和下载签到照片
5. **签到历史记录** - 查看历史签到数据

## 数据库设计

系统新增了以下数据表：

### 1. teacher_attendance_records (教师签到记录表)
- 存储教师的签到记录，包括签到状态、时间、地点等
- 支持四种签到状态：completed(已签到)、pending(未签到)、late(迟到)、missed(缺勤)

### 2. teacher_attendance_photos (签到照片表)
- 存储签到时上传的照片信息
- 支持多张照片，包含缩略图功能

### 3. teacher_attendance_statistics (签到统计表)
- 缓存统计数据，提高查询性能
- 支持日、周、月统计

## API接口

### 管理员功能

#### 1.1 获取课程列表
```
GET /teaching/courses?page=1&pageSize=12&date=2024-12-19&status=completed&search=数学
```

#### 1.2 获取课程详情
```
GET /teaching/courses/{courseId}
```

#### 1.3 获取统计数据
```
GET /teaching/statistics?date=2024-12-19&period=day
```

### 签到照片管理

#### 2.1 获取单个课程签到照片
```
GET /teaching/courses/{courseId}/attendance/photo
```

#### 2.2 获取课程签到照片列表
```
GET /teaching/courses/{courseId}/attendance/photos
```

#### 2.3 下载签到照片压缩包
```
GET /teaching/courses/{courseId}/attendance/photos/download
```

#### 2.4 下载签到报告
```
GET /teaching/courses/{courseId}/attendance/report
```

### 签到历史记录

#### 3.1 获取签到历史
```
GET /teaching/attendance/history?period=month&page=1&pageSize=20
```

## 安装和配置

### 1. 数据库初始化
运行 `teaching_system_init.sql` 文件来创建所需的数据库表：

```sql
mysql -u root -p pexitong2 < teaching_system_init.sql
```

### 2. 配置文件
在 `application.properties` 中已添加相关配置：

```properties
# 教学系统文件存储配置
teaching.file.base-path=F:/teaching_files
teaching.file.url-prefix=/api/teaching/files

# 签到时间规则配置
teaching.attendance.early-minutes=30
teaching.attendance.late-minutes=15
teaching.attendance.photo-max-size=20MB
teaching.attendance.photo-max-count=5

# 统计任务配置
teaching.statistics.auto-update=true
teaching.statistics.update-time=02:00
```

### 3. 文件存储目录
系统需要创建以下目录结构：
```
F:/teaching_files/
├── attendance_photos/     # 签到照片
├── attendance_reports/    # 签到报告
└── downloads/            # 下载文件临时目录
```

## 权限验证

每个API接口都会进行严格的权限验证：

1. **认证检查**: 验证JWT令牌的有效性
2. **角色检查**: 确保用户是校级管理员或超级管理员
3. **数据范围检查**: 校级管理员只能查看本校数据

## 错误处理

系统提供统一的错误响应格式：

```json
{
  "code": 403,
  "message": "权限不足，只有校级管理员和超级管理员才能查看教师签到信息",
  "data": null,
  "timestamp": "2024-12-19T10:00:00Z"
}
```

## 性能优化

1. **分页查询**: 所有列表接口都支持分页，避免大数据量查询
2. **索引优化**: 数据库表添加了适当的索引
3. **统计缓存**: 使用统计表缓存复杂的统计查询结果
4. **权限缓存**: 在Service层进行权限检查，减少重复验证

## 安全特性

1. **SQL注入防护**: 使用JPA参数化查询
2. **文件安全**: 文件下载接口进行路径验证
3. **权限隔离**: 严格的权限检查和数据范围控制
4. **输入验证**: 对所有输入参数进行验证

## 扩展性

系统设计考虑了未来的扩展需求：

1. **模块化设计**: 独立的package结构
2. **配置化**: 重要参数都可以通过配置文件调整
3. **接口标准化**: 遵循RESTful API设计规范
4. **数据库扩展**: 预留了院系管理员权限的扩展空间

## 注意事项

1. **教师签到功能**: 本系统只实现管理员查看功能，教师签到功能需要在其他后端实现
2. **文件下载**: 照片压缩包下载和报告下载功能预留了接口，具体实现需要根据需求完善
3. **统计任务**: 可以配置自动更新统计数据的定时任务
4. **权限扩展**: 如果需要支持院级管理员，可以修改权限检查逻辑

## 技术栈

- **后端框架**: Spring Boot 3.x
- **数据库**: MySQL 8.0+
- **ORM**: JPA/Hibernate
- **认证**: JWT
- **日志**: SLF4J + Logback
- **构建工具**: Maven




