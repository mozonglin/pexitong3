

spring.application.name=pexitong2

# 主数据库配置

spring.datasource.url=jdbc:mysql://38.207.179.218:3306/pexitongtest?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&connectTimeout=15000&socketTimeout=60000
spring.datasource.username=pexitong3
spring.datasource.password=790128mmmm
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP 连接池配置
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.maximum-pool-size=100
spring.datasource.hikari.minimum-idle=20
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.leak-detection-threshold=60000

# 时区配置
spring.jackson.time-zone=Asia/Shanghai
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
# checkuser数据库配置（通过代码配置）

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT配置
jwt.secret=mySecretKey123456789012345678901234567890
jwt.expiration=86400000

# 服务器配置
server.port=8888
server.tomcat.threads.max=300
server.tomcat.threads.min-spare=20
server.tomcat.accept-count=200

# 统计接口缓存（Caffeine，5分钟过期）
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=300s

# 文件上传配置（听课系统需要支持大文件）
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# 听课系统文件存储配置
listening.file.base-path=F:/listening_files
listening.file.url-prefix=/api/files/download

# 教学系统文件存储配置
teaching.file.base-path=C:/app_data/teaching
teaching.file.url-prefix=/api/teaching/files

# 签到时间规则配置
teaching.attendance.early-minutes=30
teaching.attendance.late-minutes=15
teaching.attendance.photo-max-size=20MB
teaching.attendance.photo-max-count=5

# 统计任务配置
teaching.statistics.auto-update=true
teaching.statistics.update-time=02:00

# 比赛成绩 Excel 上传存储路径（本地 Mac 测试用，上线时改为服务器路径）
race.excel.upload-path=yudianzhang

# 防止Bean重复定义错误
spring.main.allow-bean-definition-overriding=false

# JPA配置优化
spring.jpa.open-in-view=false

# JPush 推送配置
jpush.app-key=ccb036b62a3d7c5ab3db6367
jpush.master-secret=756a7b92b746a558bc362145
